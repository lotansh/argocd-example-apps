apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  annotations:
    metallb.universe.tf/loadBalancerIPs: 10.240.0.110
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook-ui
  type: LoadBalancer
